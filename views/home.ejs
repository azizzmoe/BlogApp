<%- include('partials/header'); -%>

    <h1>Home</h1>
    <p>
        <%= string %>
    </p>

    <% posts.forEach((post)=> { %>
        <h2>
            <%= post.title%>
        </h2>
        <p>
            <%= post.body.substring(0, 10) + `...` %> <a href="/post/<%=post.title%>">Readmore</a>
        </p>
        <% }) %>

            <%- include('partials/footer'); -%>
